extend schema @link(url: "https://specs.apollo.dev/kotlin_labs/v0.3", import: ["@typePolicy"])

extend type Query @typePolicy(embeddedFields: "users")

extend type UserConnection @typePolicy(embeddedFields: "pageInfo, edges")

extend type User @typePolicy(keyFields: "id")
